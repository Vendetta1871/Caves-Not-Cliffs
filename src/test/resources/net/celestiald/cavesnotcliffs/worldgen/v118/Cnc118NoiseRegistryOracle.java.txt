import java.io.File;
import java.io.PrintWriter;
import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;
import java.util.Map;

/** Emits built-in noise-registry fixtures from Mojang's obfuscated 1.18.2 server classes. */
public final class Cnc118NoiseRegistryOracle {
    private static final long[] SEEDS = {
        0L, 1L, -1L, 123456789L, Long.MIN_VALUE, Long.MAX_VALUE
    };

    public static void main(String[] args) throws Exception {
        ab.a();
        yv.a();
        ij.a();
        List<Map.Entry<ys<dic.a>, dic.a>> entries =
            new ArrayList<Map.Entry<ys<dic.a>, dic.a>>(ij.j.e());
        entries.sort(Comparator.comparing(entry -> entry.getKey().a().toString()));

        PrintWriter out = new PrintWriter(new File(args[0]), "UTF-8");
        out.println("# Generated by Cnc118NoiseRegistryOracle against official Minecraft Java 1.18.2 server classes");
        for (Map.Entry<ys<dic.a>, dic.a> entry : entries) {
            String name = entry.getKey().a().a();
            dic.a parameters = entry.getValue();
            out.print("parameter\t" + name + "\t" + parameters.a());
            for (double amplitude : parameters.b()) {
                out.print("\t" + bits(amplitude));
            }
            out.println();
        }
        for (long seed : SEEDS) {
            cum positional = new cux(seed).b();
            for (Map.Entry<ys<dic.a>, dic.a> entry : entries) {
                String name = entry.getKey().a().a();
                dic noise = cui.a(ij.j, positional, entry.getKey());
                out.println("sample\t" + seed + "\t" + name
                    + "\t" + bits(noise.a(12.25D, -63.5D, -9.75D))
                    + "\t" + bits(noise.a(-33554433.25D, 319.5D, 33554432.75D)));
            }
        }
        out.close();
    }

    private static String bits(double value) {
        return Long.toUnsignedString(Double.doubleToRawLongBits(value));
    }
}
